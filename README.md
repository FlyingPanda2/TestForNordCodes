# 🧪 Тесты для внутреннего Spring Boot приложения

Этот репозиторий содержит автоматизированные тесты для веб-сервиса, который обрабатывает запросы вида:

---

## 📊 Как посмотреть результаты тестирования

### 👇 Простая инструкция:

1. Нажмите зелёную кнопку **«Code» → «Download ZIP»**  

2. Распакуйте архив на свой компьютер

3. Перейдите в папку **`allure-report`**

4. **Дважды кликните по файлу `index.html`**  
   → Отчёт автоматически откроется в вашем браузере

---

## 🖼️ Пример отчёта

<img width="1919" height="988" alt="image" src="https://github.com/user-attachments/assets/1073f56c-f638-4736-8800-b99b134e805d" />


> Скриншот показывает, что все тесты прошли успешно и отчёт легко читаем.

---

## 📋 Что тестируется

| Сценарий | Описание |
|--------|--------|
| Успешный вход (`LOGIN`) | Валидный токен регистрируется |
| Действие после входа (`ACTION`) | Пользователь может выполнять операции |
| Выход из системы (`LOGOUT`) | Сессия завершается корректно |
| Удаление токена после `LOGOUT` | Токен больше не может использоваться |
| Неверный API-ключ | Возвращает `401 Unauthorized` |
| Некорректный токен | Возвращает `400 Bad Request` |
| Неизвестное действие | Возвращает `400 Bad Request` |
| Действие без входа | Возвращает `403 Forbidden` |

---

## 🛠️ Технические детали (для разработчиков)

- **Язык**: Java 17  
- **Фреймворк**: JUnit 5  
- **Мок-сервер**: WireMock (эмуляция внешнего сервиса на `localhost:8888`)  
- **Отчёт**: Allure Framework  
- **Сборка**: Maven  
- **Безопасность**: нет уязвимостей (используется встроенный `HttpClient`)

---

## ▶️ Как запустить тесты локально (опционально)

Если вы разработчик:
```bash
# 1. Запустите тестируемое приложение
java -jar -Dsecret=qazWSXedc -Dmock=http://localhost:8888/ internal-0.0.1-SNAPSHOT.jar

# 2. Запустите тесты
mvn test
